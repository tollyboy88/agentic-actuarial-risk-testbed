# Paper 1 — AAS fallback submission

This package is a fallback only; do not upload it while the NAAJ submission is active.

Before an AAS submission, complete the journal's software-stream licensing gate: confirm ownership of every source file, relicense the repository and release under GPL-3.0-only, archive that release, and replace the current version/DOI statements if the archive changes. AAS is fully open access; request the available full waiver after acceptance if an agreement does not cover the charge.

After that gate, upload `01_manuscript_AAS.docx`, `02_cover_letter_AAS.docx`, `03_replication_readme.docx`, `04_supplementary_methods.docx`, and the separate files in `figures/`.
